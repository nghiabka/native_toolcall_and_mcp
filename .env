# Local Model Base URL (OpenAI-compatible)
OPENAI_API_BASE=http://127.0.0.1:1234/v1

# Local Model API Key (Thường không cần nhưng LangChain yêu cầu chuỗi không rỗng)
OPENAI_API_KEY=local-key

# Model name
OPENAI_MODEL_NAME=local-model
